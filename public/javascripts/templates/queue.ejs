<div id="header">
    <h1>Question Queue</h1>
</div> 
<div id="queue-inner">
    <ul id="queue">
    <% _.each(questions, function (question) { %>
        <li class="question">
            <div><%= question.get('label') %></div>
            <div class="title"><%= question.get('title') %></div>
            <div><%= question.get('table_id') %></div>
        </li>
    <% }); %>
    </ul>
</div>
